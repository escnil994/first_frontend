import {SecurePipePipe} from './secure-pipe.pipe';

describe('SecurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SecurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
